# scratchpad

- Write all temporary files here. Never outside the repo (`/tmp`, `$HOME`, `..`), next to source, or in new `tmp/` dirs.
- Delete them when your task is done.
- `runs/` is `harness run`'s logs — leave alone.
